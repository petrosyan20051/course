name: Frontend
networks:
  frontend:

services:
 frontend:
    container_name: UI
    build:
      context: ../../
      dockerfile: Docker/Dockerfile
    ports:
      - 5001:81
    networks:
      - frontend